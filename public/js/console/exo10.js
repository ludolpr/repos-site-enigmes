// exercice 10 while
   
var num;

function getRandomInt(min, max){
    num = Math.floor(Math.random()* min, max);
    Math.floor(Math.random() * (max - min + 1)) + min;
}
  console.log(num);


//   function numRandom(min, max) {
//     min = Math.ceil(min);
//     max = Math.floor(max);
//     return Math.floor(Math.random() * (max - min + 1)) + min;
//   }
  
//   numRandom(1, 10);
  // renvoie par exemple : 3